---
phase: 05-booking-offene-fragen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/faq.js
autonomous: true
requirements:
  - FAQ-01
  - FAQ-02

must_haves:
  truths:
    - "Alle 8 FAQ-Eintraege werden als Checkliste gerendert"
    - "Status 'open' zeigt rotes 🔴-Icon, 'resolved' zeigt gruenes 🟢-Icon"
    - "Prioritaet ist farblich codiert (critical = rot, high = gelb)"
    - "Kein Click-Handler fuer Status-Toggle — nur Read aus data.js"
    - "tip-Feld wird angezeigt wenn vorhanden, answer-Feld wenn nicht null"
  artifacts:
    - path: "js/faq.js"
      provides: "IIFE-Modul window.faqModule mit init()"
      exports: ["window.faqModule"]
  key_links:
    - from: "js/faq.js"
      to: "window.TAIWAN.faq"
      via: "init() liest faq[] Array"
      pattern: "window\\.TAIWAN\\.faq"
---

<objective>
Baut js/faq.js als IIFE-Modul nach restaurants.js-Muster. Rendert alle 8 FAQ-Eintraege als read-only Checkliste mit Status-Icon und Prioritaets-Farbe. Kein Toggle-Mechanismus — Status kommt ausschliesslich aus data.js.

Purpose: FAQ-01 und FAQ-02 implementieren. Nutzer sieht alle offenen Fragen mit Status auf einen Blick.
Output: js/faq.js (neu)
</objective>

<execution_context>
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.planning/phases/05-booking-offene-fragen/05-RESEARCH.md
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.planning/phases/05-booking-offene-fragen/05-PATTERNS.md

<interfaces>
<!-- Aus js/data.js — faq[] Shape (PATTERNS.md lines 158-169 verifiziert) -->

faq[i] shape:
  id: String (z.B. "faq-001")
  question: String
  answer: String|null  -- null bei allen 8 (noch offen)
  status: String       -- "open" | "resolved"  (aktuell alle "open")
  phaseIds: Array      -- welche Reisephasen betroffen
  priority: String     -- "critical" | "high" | "medium" | "low"
  tip: String          -- Recherche-Hinweis (immer vorhanden)

<!-- FAQ-02 Constraint: Status nur aus data.js lesen, KEIN Runtime-Toggle -->
<!-- Mark aendert status in data.js manuell => Reload zeigt neuen Status -->

<!-- Aus js/restaurants.js — IIFE-Pattern -->
window.restaurantsModule = (function() {
  'use strict';
  return {
    init: function() {
      if (!window.TAIWAN || !window.TAIWAN.restaurants) return;
      var el = document.getElementById('restaurants-content');
      if (!el) return;
      el.innerHTML = '';
      _render(el);
      console.log('[restaurants.js] initialisiert');
    }
  };
})();

<!-- CSS aus PATTERNS.md — faq-* Klassen (werden in Plan 05-03 ergaenzt) -->
.faq-item--open    { border-left: 3px solid #EF4444; }
.faq-item--resolved { border-left: 3px solid #22C55E; }
.faq-priority--critical { color: #EF4444; }
.faq-priority--high     { color: #F59E0B; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: js/faq.js — IIFE-Modul mit FAQ-Checkliste</name>
  <files>js/faq.js</files>
  <action>
Neue Datei js/faq.js erstellen. Exakt nach restaurants.js-IIFE-Muster (var, function(){}, keine Arrow Functions, keine Template Literals, kein const/let).

Struktur:
- window.faqModule = IIFE
- init(): Guard auf window.TAIWAN und window.TAIWAN.faq + Element 'faq-content'. el.innerHTML = ''. Dann _render(el) aufrufen. console.log('[faq.js] initialisiert (' + window.TAIWAN.faq.length + ' Fragen)').
- _render(container): Baut '<div class="faq-list">' + alle Items + '</div>', dann insertAdjacentHTML('beforeend', html). NICHT innerHTML +=.
- _makeFaqItem(q): Baut HTML-String fuer eine FAQ-Zeile:
  1. statusIcon: q.status === 'resolved' dann '&#x1F7E2;' (gruener Kreis) sonst '&#x1F534;' (roter Kreis)
  2. statusLabel: q.status === 'resolved' dann 'Gekl&#xE4;rt' sonst 'Offen'
  3. Header-Zeile: statusIcon + statusLabel + Prioritaets-Span mit class='faq-priority faq-priority--' + q.priority und Text = q.priority
  4. Frage-Text: '<div class="faq-question">' + q.question + '</div>'
  5. Tip (wenn vorhanden): '<div class="faq-tip">&#x1F4A1; ' + q.tip + '</div>'
  6. Answer (wenn nicht null): '<div class="faq-answer">&#x2714; ' + q.answer + '</div>'
  7. Ganzes Item: '<div class="faq-item faq-item--' + q.status + '">' + ... + '</div>'

WICHTIG: Kein addEventListener, kein onclick, kein status-Toggle. Nur lesen, nie schreiben.
Kommentar in Code: // FAQ-Status hier aendern: data.js faq[].status = "open" | "resolved"
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'); var c=fs.readFileSync('js/faq.js','utf8'); if(c.indexOf('window.faqModule')===-1) throw new Error('MISSING: window.faqModule'); if(c.indexOf('faq-item--')===-1) throw new Error('MISSING: faq-item class'); if(c.indexOf('addEventListener')!==-1) throw new Error('FORBIDDEN: event listener found (no toggle allowed)'); if(c.indexOf('1F534')===-1) throw new Error('MISSING: red circle icon'); if(c.indexOf('const')!==-1||c.indexOf('=>')!==-1) throw new Error('ES6 syntax found'); console.log('faq.js: OK');"</automated>
  </verify>
  <done>js/faq.js existiert mit window.faqModule, rendert 8 FAQ-Items mit Status-Icons und Prioritaets-Farbe. Kein Click-Handler vorhanden. ES5-konform.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.js → DOM | faq[].question und faq[].tip aus data.js werden direkt in innerHTML eingesetzt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | faq.js DOM-Render | accept | Statische App, data.js ist kontrollierte Quelle (kein User-Input), kein XSS-Risiko |
| T-05-04 | Information Disclosure | FAQ-Inhalte | accept | Lokal oder hinter Passwort-Gate (Phase 6), FAQ-Inhalte sind nicht sicherheitskritisch |
</threat_model>

<verification>
1. node -e "require('./js/faq.js')" — kein Syntaxfehler
2. Browser nach Wave-2-Integration: 8 FAQ-Items sichtbar im Fragen-Abschnitt
3. Alle Items zeigen rotes 🔴 Icon (alle status: "open")
4. Prioritaet "critical" und "high" farblich hervorgehoben
5. tip-Text unter jeder Frage sichtbar
6. Kein Button oder Toggle im UI
</verification>

<success_criteria>
- FAQ-01: 8 Fragen als Checkliste mit 🔴 Status sichtbar
- FAQ-02: Wenn status in data.js auf "resolved" gesetzt wird, zeigt Reload 🟢
- Kein Runtime-Toggle (nur data.js-Aenderung bewirkt Status-Wechsel)
- ES5-konform: var, function(){}, keine Arrow Functions
</success_criteria>

<output>
Nach Abschluss: .planning/phases/05-booking-offene-fragen/05-02-SUMMARY.md erstellen mit erledigten Requirements und Hinweis auf naechsten Plan (05-03 Integration).
</output>
