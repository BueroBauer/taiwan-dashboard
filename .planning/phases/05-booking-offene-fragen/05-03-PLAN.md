---
phase: 05-booking-offene-fragen
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - index.html
  - js/app.js
  - css/style.css
autonomous: true
requirements:
  - BKG-01
  - BKG-02
  - BKG-03
  - BKG-04
  - BKG-05
  - FAQ-01
  - FAQ-02

must_haves:
  truths:
    - "index.html hat Script-Tags fuer booking.js und faq.js nach data.js und vor app.js"
    - "app.js ruft bookingModule.init() und faqModule.init() in DOMContentLoaded auf"
    - "Booking-Section und FAQ-Section in index.html haben korrekte Container-IDs"
    - "css/style.css hat .faq-* Klassen"
    - "Dashboard oeffnet im Browser ohne Console-Fehler, beide Sektionen sichtbar"
  artifacts:
    - path: "index.html"
      provides: "Script-Tags fuer booking.js + faq.js, Section-Container"
      contains: "booking.js"
    - path: "js/app.js"
      provides: "bookingModule.init() und faqModule.init() Aufrufe"
      contains: "bookingModule"
    - path: "css/style.css"
      provides: ".faq-* Klassen"
      contains: ".faq-item"
  key_links:
    - from: "index.html script tags"
      to: "js/booking.js + js/faq.js"
      via: "Script-Reihenfolge: nach data.js, vor app.js"
      pattern: "booking\\.js"
    - from: "js/app.js DOMContentLoaded"
      to: "window.bookingModule.init()"
      via: "Guard-Pattern"
      pattern: "bookingModule"
---

<objective>
Verdrahtet die in Wave 1 erstellten Module booking.js und faq.js mit dem bestehenden Dashboard. Drei chirurgische Eingriffe: index.html Script-Tags, app.js init()-Aufrufe, css/style.css faq-* Klassen.

Purpose: Ohne diese Integration laufen booking.js und faq.js nie. Wave 1 baut die Module, Wave 2 schaltet sie ein.
Output: Vollstaendig integriertes Dashboard mit Booking- und FAQ-Sektion.
</objective>

<execution_context>
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.planning/phases/05-booking-offene-fragen/05-PATTERNS.md
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.planning/phases/05-booking-offene-fragen/05-01-SUMMARY.md
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.planning/phases/05-booking-offene-fragen/05-02-SUMMARY.md

<interfaces>
<!-- Aus PATTERNS.md — bestehende Script-Reihenfolge in index.html (lines 218-228) -->
Bestehende Reihenfolge:
  <script src="js/data.js"></script>
  <script src="js/map.js"></script>
  <script src="js/charts.js"></script>
  <script src="js/videos.js"></script>
  <script src="js/restaurants.js"></script>
  <!-- HIER: booking.js und faq.js einfuegen -->
  <script src="js/app.js"></script>  <!-- MUSS last bleiben -->

<!-- Aus PATTERNS.md — bestehende DOMContentLoaded in app.js (lines 194-209) -->
document.addEventListener('DOMContentLoaded', function() {
  initGate();
  initNav();
  if (window.mapModule)         { window.mapModule.init(); }
  if (window.chartsModule)      { window.chartsModule.init(); }
  if (window.videosModule)      { window.videosModule.init(); }
  if (window.restaurantsModule) { window.restaurantsModule.init(); }
  // HIER: bookingModule und faqModule ergaenzen
  renderTimeline();
});

<!-- Aus PATTERNS.md — bestehende Section-Container in index.html -->
<section id="booking" class="section">
  <div class="section-header">
    <h2 class="section-title">Booking</h2>
  </div>
  <div id="booking-content" class="placeholder">...</div>
</section>

<section id="fragen" class="section">
  <div class="section-header">
    <h2 class="section-title">Offene Fragen</h2>
  </div>
  <div id="faq-content" class="placeholder">...</div>
</section>

Hinweis: Wenn Section-Container noch nicht in index.html vorhanden: ergaenzen.
Wenn vorhanden: unveraendert lassen, nur Script-Tags ergaenzen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html — Script-Tags + Section-Container pruefen/ergaenzen</name>
  <files>index.html</files>
  <action>
Zwei Aktionen in index.html:

1. SCRIPT-TAGS: Pruefe ob booking.js und faq.js Script-Tags vorhanden sind.
   Falls nicht: Nach dem restaurants.js Script-Tag und VOR dem app.js Script-Tag einfuegen:
     <script src="js/booking.js"></script>
     <script src="js/faq.js"></script>
   Reihenfolge MUSS sein: data.js → ... → restaurants.js → booking.js → faq.js → app.js
   Kein Script-Tag nach app.js ergaenzen.

2. SECTION-CONTAINER: Pruefe ob folgende Elemente in index.html vorhanden sind:
   - Element mit id="booking-content"
   - Element mit id="faq-content"
   Falls nicht vorhanden: Sections hinzufuegen (nach dem bestehenden restaurants-Section-Block):

   <section id="booking" class="section">
     <div class="section-header">
       <h2 class="section-title">&#x1F3E8; Booking</h2>
     </div>
     <div id="booking-content" class="placeholder">Wird geladen&hellip;</div>
   </section>

   <section id="fragen" class="section">
     <div class="section-header">
       <h2 class="section-title">&#x2753; Offene Fragen</h2>
     </div>
     <div id="faq-content" class="placeholder">Wird geladen&hellip;</div>
   </section>

   Falls vorhanden: unveraendert lassen (keine Dopplung erstellen).

CHIRURGISCH: Nur die zwei spezifischen Stellen anfassen. Keinen anderen HTML-Code veraendern.
  </action>
  <verify>
    <automated>grep -c "booking\.js" index.html && grep -c "faq\.js" index.html && grep -c "booking-content" index.html && grep -c "faq-content" index.html</automated>
  </verify>
  <done>index.html hat Script-Tags fuer booking.js + faq.js in korrekter Reihenfolge (nach restaurants.js, vor app.js). Container-IDs booking-content und faq-content existieren.</done>
</task>

<task type="auto">
  <name>Task 2: app.js + css/style.css — Module aktivieren und FAQ-CSS ergaenzen</name>
  <files>js/app.js, css/style.css</files>
  <action>
ZWEI Dateien, ZWEI chirurgische Eingriffe:

**js/app.js:**
Suche den DOMContentLoaded-Block. Finde die Zeile mit restaurantsModule.init(). Direkt DANACH (vor renderTimeline()) diese zwei Zeilen ergaenzen:
  if (window.bookingModule) { window.bookingModule.init(); }
  if (window.faqModule)     { window.faqModule.init(); }

Guard-Pattern zwingend (if (window.X)) — identisch zu allen anderen Modul-Aufrufen.
Keinen anderen Code in app.js veraendern.

**css/style.css:**
Am Ende der Datei (nach dem .bkg-* Block aus Plan 05-01) die .faq-* Klassen ergaenzen:

/* === FAQ === */
.faq-list { display: flex; flex-direction: column; gap: 0.5rem; }

.faq-item { background: var(--card); border: 1px solid var(--border); border-radius: var(--radius); padding: 0.75rem 1rem; display: flex; flex-direction: column; gap: 0.35rem; }
.faq-item--open     { border-left: 3px solid #EF4444; }
.faq-item--resolved { border-left: 3px solid #22C55E; }

.faq-header { display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap; }
.faq-status { font-size: 0.82rem; font-weight: 600; color: var(--text); }
.faq-priority { font-size: 0.7rem; font-weight: 700; text-transform: uppercase; padding: 0.1rem 0.3rem; border-radius: 3px; background: var(--border); }
.faq-priority--critical { color: #EF4444; }
.faq-priority--high     { color: #F59E0B; }
.faq-priority--medium   { color: var(--muted); }
.faq-priority--low      { color: var(--muted); opacity: 0.6; }

.faq-question { color: var(--text); font-size: 0.9rem; font-weight: 500; line-height: 1.4; }
.faq-tip      { color: var(--muted); font-size: 0.78rem; line-height: 1.4; }
.faq-answer   { color: #22C55E; font-size: 0.82rem; line-height: 1.4; }
  </action>
  <verify>
    <automated>grep -c "bookingModule" js/app.js && grep -c "faqModule" js/app.js && grep -c "faq-item--open" css/style.css</automated>
  </verify>
  <done>app.js ruft bookingModule.init() und faqModule.init() auf. css/style.css hat alle .faq-* Klassen mit korrekten Statusfarben.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Wave 1 + Wave 2 vollstaendig: booking.js + faq.js als Module, CSS-Klassen, Script-Tags, init()-Aufrufe. Dashboard komplett integriert.
  </what-built>
  <how-to-verify>
1. index.html im Browser oeffnen (file:// reicht)
2. Zur Booking-Sektion scrollen: 10 Cards sichtbar, pro Phase Region + Dates + Links
3. Phase 5 (Xiaoliuqiu), Phase 6 (Kenting), Phase 8 (Alishan) zeigen rotes "Frueh buchen!" Badge
4. Mindestens eine Card zeigt KKday-Tour mit gelbem Platzhalter-Hinweis (statt Link)
5. Alle Cards zeigen "Hotels noch nicht eingetragen" (exampleHotels leer)
6. Zur Fragen-Sektion scrollen: 8 FAQ-Items sichtbar, alle mit rotem 🔴 Icon
7. Jede Frage hat tip-Text darunter
8. Browser Console (F12): keine Fehler
  </how-to-verify>
  <resume-signal>Schreibe "ok" wenn alles korrekt, oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script-Reihenfolge | data.js muss vor booking.js/faq.js geladen werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Script-Ladereihenfolge | mitigate | booking.js + faq.js nach data.js einfuegen, Guard-Pattern in app.js verhindert undefined-Fehler |
| T-05-06 | Tampering | index.html Sections | accept | Statische Datei, kein User-Input in HTML-Struktur |
</threat_model>

<verification>
1. grep -c "booking.js" index.html -- muss >= 1 sein
2. grep -c "bookingModule" js/app.js -- muss >= 1 sein
3. grep -c "faq-item--open" css/style.css -- muss >= 1 sein
4. Browser-Test: 10 Booking-Cards + 8 FAQ-Items sichtbar, keine Console-Fehler
</verification>

<success_criteria>
- Alle 7 Requirements (BKG-01..05, FAQ-01..02) visuell verifiziert
- Dashboard laedt ohne JavaScript-Fehler
- Script-Reihenfolge korrekt (data.js vor booking/faq, app.js last)
- Human-Checkpoint bestaetigt visuelles Ergebnis
</success_criteria>

<output>
Nach Abschluss: .planning/phases/05-booking-offene-fragen/05-03-SUMMARY.md erstellen. Phase 5 als abgeschlossen markieren in STATE.md.
</output>
