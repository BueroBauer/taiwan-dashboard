---
phase: 04-infografiken-videos-restaurants
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - index.html
  - js/app.js
  - css/style.css
autonomous: true
requirements:
  - CHT-01
  - CHT-02
  - CHT-03
  - CHT-04
  - CHT-05
  - VID-01
  - VID-02
  - VID-03
  - VID-04
  - RST-01
  - RST-02

must_haves:
  truths:
    - "index.html lädt videos.js + restaurants.js vor app.js"
    - "app.js ruft chartsModule.init(), videosModule.init(), restaurantsModule.init() auf"
    - "Canvas-Elemente chart-costs/nights/temp/entries + budget-counter-container in #zahlen vorhanden"
    - "Abschnitt #videos enthält videos-content Container (bereits vorhanden)"
    - "Alle 4 Charts sichtbar nach Seitenload"
    - "Video-Grid rendert beim ersten Seitenload (Tab 'Alle')"
    - "Restaurant-Filter und Cards sichtbar"
    - "Dark-Theme konsistent: alle neuen Elemente nutzen CSS Custom Properties"
  artifacts:
    - path: "index.html"
      provides: "2 neue script-Tags + Canvas-HTML in #zahlen"
      contains: "videos.js"
    - path: "js/app.js"
      provides: "3 neue module.init()-Aufrufe in DOMContentLoaded"
      contains: "videosModule"
    - path: "css/style.css"
      provides: "Styles für .chart-grid, .vid-tabs, .vid-grid, .rst-grid, .budget-counter"
      contains: "chart-grid"
  key_links:
    - from: "index.html script-Tags"
      to: "window.videosModule / window.restaurantsModule"
      via: "Script-Load-Reihenfolge"
      pattern: "videos\\.js"
    - from: "app.js DOMContentLoaded"
      to: "window.chartsModule.init()"
      via: "if (window.chartsModule) window.chartsModule.init()"
      pattern: "chartsModule"
---

<objective>
Integration: index.html mit Canvas-HTML + Script-Tags erweitern, app.js um 3 Module-Init-Aufrufe ergänzen, css/style.css mit allen neuen Styles für Charts/Videos/Restaurants/Budget erweitern.

Purpose: Verbindet die drei Wave-1-Module mit dem DOM. Erst nach diesem Plan ist Phase 4 vollständig funktionsfähig.
Output: Lauffähige App mit allen Phase-4-Features.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-infografiken-videos-restaurants/04-RESEARCH.md
@.planning/phases/04-infografiken-videos-restaurants/04-PATTERNS.md
@.planning/phases/04-infografiken-videos-restaurants/04-01-SUMMARY.md
@.planning/phases/04-infografiken-videos-restaurants/04-02-SUMMARY.md
@.planning/phases/04-infografiken-videos-restaurants/04-03-SUMMARY.md

<interfaces>
<!-- Aktuelle Script-Reihenfolge in index.html (Zeile 150–153) -->
// <script src="js/data.js"></script>
// <script src="js/map.js"></script>
// <script src="js/charts.js"></script>
// <script src="js/app.js"></script>

<!-- Soll-Zustand nach diesem Plan -->
// <script src="js/data.js"></script>
// <script src="js/map.js"></script>
// <script src="js/charts.js"></script>
// <script src="js/videos.js"></script>       NEU
// <script src="js/restaurants.js"></script>  NEU
// <script src="js/app.js"></script>

<!-- Aktuelle Placeholder-Divs in index.html -->
// <div id="charts-content" class="placeholder">Infografiken · kommt in Phase 4</div>
// <div id="videos-content" class="placeholder">YouTube-Guide · kommt in Phase 4</div>
// <div id="restaurants-content" class="placeholder">Restaurant-Guide · kommt in Phase 4</div>

<!-- app.js DOMContentLoaded (aktuell, Zeile ~260–268) -->
// document.addEventListener('DOMContentLoaded', function() {
//   initGate();
//   initNav();
//   if (window.mapModule) { window.mapModule.init(); }
//   renderTimeline();
// });

<!-- CSS Custom Properties (css/style.css Zeile 11–36) -->
// --bg: #0D0F12
// --card: #161A20
// --border: #1F2937
// --accent: #3B82F6
// --text: #E5E7EB
// --muted: #9CA3AF
// --radius: 14px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.html — Script-Tags + Canvas-HTML</name>
  <files>index.html</files>
  <read_first>
    - index.html (vollständig lesen — Script-Block Zeile 147–153 + Section #zahlen Zeile 102–108 + Sections #videos, #restaurants)
    - .planning/phases/04-infografiken-videos-restaurants/04-RESEARCH.md (Code Examples: Canvas-HTML, Script-Reihenfolge)
  </read_first>
  <action>
    Zwei Änderungen an index.html (chirurgisch — nichts anderes anfassen):

    ÄNDERUNG 1: Script-Tags
    Vor `<script src="js/app.js"></script>` zwei neue Tags einfügen:
    ```html
    <script src="js/videos.js"></script>
    <script src="js/restaurants.js"></script>
    ```
    Reihenfolge danach: data.js → map.js → charts.js → videos.js → restaurants.js → app.js

    ÄNDERUNG 2: Canvas-HTML in #zahlen
    Den bestehenden Placeholder-Div ersetzen:
    ```html
    <!-- ALT: -->
    <div id="charts-content" class="placeholder">Infografiken · kommt in Phase 4</div>

    <!-- NEU: -->
    <div id="charts-content">
      <div class="chart-grid">
        <div class="chart-card">
          <h3 class="chart-title">Kostenverteilung</h3>
          <div class="chart-container" style="position:relative;height:300px">
            <canvas id="chart-costs"></canvas>
          </div>
        </div>
        <div class="chart-card">
          <h3 class="chart-title">Nächte pro Phase</h3>
          <div class="chart-container" style="position:relative;height:300px">
            <canvas id="chart-nights"></canvas>
          </div>
        </div>
        <div class="chart-card">
          <h3 class="chart-title">Temperaturen (Min/Max)</h3>
          <div class="chart-container" style="position:relative;height:300px">
            <canvas id="chart-temp"></canvas>
          </div>
        </div>
        <div class="chart-card">
          <h3 class="chart-title">Eintritte — Gratis vs. Kosten</h3>
          <div class="chart-container" style="position:relative;height:300px">
            <canvas id="chart-entries"></canvas>
          </div>
        </div>
      </div>
      <div id="budget-counter-container"></div>
    </div>
    ```

    NICHT ändern:
    - #videos-content und #restaurants-content (werden von Modulen befüllt)
    - Leaflet CDN, Chart.js CDN, Leaflet-MarkerCluster CDN
    - Irgendwas außerhalb der zwei Änderungspunkte
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('index.html','utf8'); ['js/videos.js','js/restaurants.js','chart-costs','chart-nights','chart-temp','chart-entries','budget-counter-container','chart-grid'].forEach(t=>{if(!h.includes(t)){console.error('MISSING: '+t);process.exit(1);}console.log('OK: '+t);}); var scripts=h.match(/script src=\"js\/.+?\"/g)||[]; var order=['js/data.js','js/map.js','js/charts.js','js/videos.js','js/restaurants.js','js/app.js']; order.forEach(function(s){if(h.indexOf(s)===-1){console.error('MISSING SCRIPT: '+s);process.exit(1);}console.log('SCRIPT OK: '+s);});"</automated>
  </verify>
  <acceptance_criteria>
    - videos.js und restaurants.js als script-Tags vorhanden, VOR app.js
    - 4 canvas-Elemente mit IDs chart-costs/nights/temp/entries vorhanden
    - budget-counter-container vorhanden
    - chart-grid und chart-card Wrapper vorhanden
    - Leaflet/Chart.js CDN unverändert
    - Placeholder-Klasse bei charts-content entfernt
  </acceptance_criteria>
  <done>index.html liefert Canvas-Elemente für chartsModule + lädt alle 3 Module vor app.js</done>
</task>

<task type="auto">
  <name>Task 2: app.js + css/style.css — Init-Aufrufe + neue Styles</name>
  <files>js/app.js, css/style.css</files>
  <read_first>
    - js/app.js (DOMContentLoaded-Block am Dateiende lesen — ca. letzte 15 Zeilen)
    - css/style.css (letzte 30 Zeilen + Zeilen 11–36 für CSS Custom Properties)
    - .planning/phases/04-infografiken-videos-restaurants/04-PATTERNS.md (CSS-Patterns: .chart-grid, .vid-tabs, .rst-filters, .budget-counter)
  </read_first>
  <action>
    ÄNDERUNG app.js — DOMContentLoaded-Block erweitern:
    Nach `if (window.mapModule) { window.mapModule.init(); }` drei Zeilen einfügen:
    ```javascript
    if (window.chartsModule)      { window.chartsModule.init(); }
    if (window.videosModule)      { window.videosModule.init(); }
    if (window.restaurantsModule) { window.restaurantsModule.init(); }
    ```
    `renderTimeline()` bleibt an letzter Stelle. Nichts anderes in app.js ändern.

    ÄNDERUNG css/style.css — neue Styles am Ende anhängen:
    Neuen Kommentar-Block anhängen: `/* === Phase 4: Charts / Videos / Restaurants === */`

    Chart-Styles:
    ```css
    .chart-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }
    @media (min-width: 768px) {
      .chart-grid { grid-template-columns: 1fr 1fr; }
    }
    .chart-card {
      background-color: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.25rem;
    }
    .chart-title {
      color: var(--text);
      font-size: 0.9rem;
      font-weight: 600;
      margin: 0 0 0.75rem;
    }
    .chart-container canvas {
      display: block;
    }
    ```

    Budget-Counter-Styles:
    ```css
    .budget-counter {
      display: flex;
      align-items: center;
      gap: 1.5rem;
      flex-wrap: wrap;
      background-color: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1.25rem 1.5rem;
      margin-top: 1rem;
    }
    .budget-item {
      text-align: center;
      flex: 1;
      min-width: 120px;
    }
    .budget-amount {
      font-size: 2rem;
      font-weight: 700;
      color: var(--accent);
    }
    .budget-label {
      font-size: 0.8rem;
      color: var(--muted);
      margin-top: 0.25rem;
    }
    .budget-vs {
      color: var(--muted);
      font-size: 0.9rem;
      font-weight: 600;
    }
    .budget-total {
      width: 100%;
      text-align: center;
      font-size: 0.85rem;
      color: var(--muted);
      margin-top: 0.5rem;
    }
    ```

    Video-Styles:
    ```css
    .vid-tabs {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }
    .vid-tab {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--muted);
      cursor: pointer;
      font-size: 0.8rem;
      padding: 0.3rem 0.75rem;
    }
    .vid-tab--active,
    .vid-tab:hover {
      background: var(--accent);
      border-color: var(--accent);
      color: #fff;
    }
    .vid-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    @media (min-width: 768px) {
      .vid-grid { grid-template-columns: repeat(3, 1fr); }
    }
    .vid-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      overflow: hidden;
      cursor: pointer;
    }
    .vid-thumb-wrap {
      position: relative;
      aspect-ratio: 16/9;
      background: var(--border);
      overflow: hidden;
    }
    .vid-thumb {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
    .vid-thumb-placeholder {
      width: 100%;
      height: 100%;
      background: #1F2937;
    }
    .vid-thumb-wrap iframe {
      width: 100%;
      height: 100%;
      border: none;
      display: block;
    }
    .vid-play-btn {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: rgba(0,0,0,0.6);
      color: #fff;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1rem;
      pointer-events: none;
    }
    .vid-title {
      padding: 0.6rem 0.75rem;
      font-size: 0.8rem;
      color: var(--text);
      line-height: 1.3;
    }
    ```

    Restaurant-Styles:
    ```css
    .rst-filters {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }
    .rst-filter {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--muted);
      cursor: pointer;
      font-size: 0.78rem;
      padding: 0.3rem 0.65rem;
    }
    .rst-filter--active,
    .rst-filter:hover {
      background: var(--accent);
      border-color: var(--accent);
      color: #fff;
    }
    .rst-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }
    @media (min-width: 768px) {
      .rst-grid { grid-template-columns: 1fr 1fr; }
    }
    .rst-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 1rem;
      display: flex;
      flex-direction: column;
      gap: 0.4rem;
    }
    .rst-name {
      color: var(--text);
      font-weight: 600;
      font-size: 0.95rem;
    }
    .rst-meta {
      display: flex;
      gap: 0.75rem;
      align-items: center;
    }
    .rst-type {
      color: var(--muted);
      font-size: 0.78rem;
    }
    .rst-price {
      color: var(--accent);
      font-size: 0.78rem;
      font-weight: 600;
    }
    .rst-note {
      color: var(--muted);
      font-size: 0.78rem;
      line-height: 1.4;
    }
    .rst-link {
      color: var(--accent);
      font-size: 0.78rem;
      text-decoration: none;
      margin-top: 0.25rem;
    }
    .rst-link:hover {
      text-decoration: underline;
    }
    ```

    Wichtig:
    - Nur anhängen an css/style.css — nichts bestehende ändern
    - Nur die 3 Zeilen in app.js DOMContentLoaded einfügen — nichts sonst
    - Alle CSS-Farben via var(--X), keine Hex-Werte außer wo custom properties fehlen
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const app=fs.readFileSync('js/app.js','utf8'); ['chartsModule.init','videosModule.init','restaurantsModule.init'].forEach(t=>{if(!app.includes(t)){console.error('MISSING in app.js: '+t);process.exit(1);}console.log('OK app.js: '+t);}); const css=fs.readFileSync('css/style.css','utf8'); ['chart-grid','vid-tabs','vid-grid','rst-grid','budget-counter','vid-thumb-placeholder','rst-filter--active'].forEach(t=>{if(!css.includes(t)){console.error('MISSING in css: '+t);process.exit(1);}console.log('OK css: '+t);});"</automated>
  </verify>
  <acceptance_criteria>
    - app.js: chartsModule.init() + videosModule.init() + restaurantsModule.init() im DOMContentLoaded-Block
    - app.js: renderTimeline() bleibt letzte Zeile vor dem Schließen
    - css/style.css: .chart-grid, .chart-card, .chart-title vorhanden
    - css/style.css: .vid-tabs, .vid-tab, .vid-tab--active, .vid-grid, .vid-card vorhanden
    - css/style.css: .rst-filters, .rst-filter, .rst-grid, .rst-card vorhanden
    - css/style.css: .budget-counter, .budget-amount, .budget-label vorhanden
    - css/style.css: .vid-thumb-placeholder vorhanden (grauer Fallback)
    - Responsive: @media (min-width: 768px) für chart-grid (2col), vid-grid (3col), rst-grid (2col)
    - Farben via var(--card), var(--border), var(--accent), var(--text), var(--muted)
  </acceptance_criteria>
  <done>app.js triggert alle 3 Module, css/style.css hat vollständiges Dark-Theme für Phase-4-Elemente — App ist lauffähig</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.html → CDN Scripts | Chart.js/Leaflet von externen CDNs geladen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | CDN-Scripts | accept | Versionsgepinnte URLs (chart.js@4.4.0, leaflet@1.9.4); für Familien-Dashboard akzeptables Risiko |
</threat_model>

<verification>
Browser: index.html via file:// oder localhost öffnen.
1. #zahlen: 4 Charts sichtbar (Donut, Balken, Linie, Scatter) + Budget-Counter €145 vs €29
2. #videos: 9 Tabs, 20 Cards mit Placeholder-Thumbnails
3. #restaurants: Filter-Buttons "Alle" + 10 Phasen, 15 Cards mit Maps-Links
4. DevTools Responsive (375px): Charts gestackt, Video-Grid 1-spaltig, Tabs gewrapped
Manuell pro CHT/VID/RST-Requirement verifizieren (Checkliste in RESEARCH.md Abschnitt "Manuelle Testcheckliste").
</verification>

<success_criteria>
index.html lädt alle 5 JS-Module korrekt. app.js initialisiert alle 3 Phase-4-Module. Alle 11 Requirements (CHT-01..05, VID-01..04, RST-01..02) visuell erfüllt. Dark-Theme konsistent.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-infografiken-videos-restaurants/04-04-SUMMARY.md` erstellen.
</output>
