---
phase: 04-infografiken-videos-restaurants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/charts.js
autonomous: true
requirements:
  - CHT-01
  - CHT-02
  - CHT-03
  - CHT-04
  - CHT-05

must_haves:
  truths:
    - "Donut-Chart mit 7 Kostenverteilungs-Segmenten ist sichtbar in #zahlen"
    - "Balkendiagramm zeigt 10 farbcodierte Phasen-Nächte"
    - "Liniendiagramm zeigt Min/Max-Temperaturkurve, Alishan-Dip bei Phase 8 erkennbar"
    - "Scatter-Chart zeigt Gratis-Sights (grün) vs. Eintrittspreise (blau), Tooltip mit Namen"
    - "Budget-Counter zeigt €145 (Komfort) vs. €29 (Backpacker) als styled HTML-Block"
  artifacts:
    - path: "js/charts.js"
      provides: "window.chartsModule IIFE mit init(), 4 Chart-Instanzen + Budget-Counter"
      min_lines: 120
      exports: ["window.chartsModule"]
      contains: "chartsModule"
  key_links:
    - from: "js/charts.js"
      to: "window.TAIWAN.charts"
      via: "direkter Zugriff in init()"
      pattern: "window\\.TAIWAN\\.charts"
    - from: "js/charts.js"
      to: "canvas#chart-costs"
      via: "document.getElementById('chart-costs')"
      pattern: "chart-costs"
---

<objective>
`js/charts.js` vollständig implementieren: 4 Chart.js-Visualisierungen (Donut, Bar, Line, Scatter) + HTML-Budget-Counter. Ersetzt den bestehenden Stub.

Purpose: Kerninfografiken der Taiwan-Reise — Kosten, Zeitverteilung, Temperaturen, Eintritte.
Output: `js/charts.js` als IIFE-Modul mit `window.chartsModule.init()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-infografiken-videos-restaurants/04-RESEARCH.md
@.planning/phases/04-infografiken-videos-restaurants/04-PATTERNS.md

<interfaces>
<!-- Bestehende Datenstruktur aus window.TAIWAN (data.js) -->

window.TAIWAN.charts.costs    = { labels: [...], values: [...], colors: [...] }
window.TAIWAN.charts.nights   = { labels: [...], values: [...], colors: [...] }
window.TAIWAN.charts.temperature = { labels: [...], tempMin: [...], tempMax: [...] }
window.TAIWAN.charts.entries  = { data: [{ label, gratis, eur }, ...] }
window.TAIWAN.meta.dailyBudget = { comfort: 145, backpacker: 29 }
window.TAIWAN.meta.budget      = { low: ..., high: ... }

<!-- Bestehender Stub (wird ersetzt) -->
window.chartsModule = { init: function() { ... } }  // js/charts.js Zeile 5

<!-- Canvas-IDs die in index.html existieren (Plan 04-04 ergänzt sie) -->
// chart-costs, chart-nights, chart-temp, chart-entries, budget-counter-container
// HINWEIS: Diese Canvas-IDs existieren noch NICHT in index.html.
// charts.js muss Guard-Checks haben: if (!canvas) return;
// Plan 04-04 (Wave 2) schreibt das HTML in index.html.
// charts.js trotzdem jetzt implementieren — init() wird von app.js aufgerufen
// nachdem index.html die Canvas-Elemente hat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: charts.js — IIFE-Modul mit 4 Charts + Budget-Counter</name>
  <files>js/charts.js</files>
  <read_first>
    - js/charts.js (aktueller Stub — vollständig ersetzen)
    - js/map.js (IIFE-Pattern, private state, public return)
    - .planning/phases/04-infografiken-videos-restaurants/04-PATTERNS.md (alle Chart-Patterns, exakte Code-Snippets)
  </read_first>
  <action>
    Ersetze den gesamten Inhalt von `js/charts.js` mit einem IIFE-Modul nach dem map.js-Pattern.

    Struktur:
    ```
    window.chartsModule = (function() {
      'use strict';
      var _charts = {};
      // private Hilfsfunktionen
      return { init: function() { ... } };
    })();
    ```

    Reihenfolge in init():
    1. Guard: `if (!window.TAIWAN) return;`
    2. `Chart.defaults.color = '#9CA3AF';` / `Chart.defaults.borderColor = '#1F2937';`
    3. `_renderBudgetCounter()` (kein Canvas)
    4. `_initCosts()` — Donut (CHT-01)
    5. `_initNights()` — Bar (CHT-02)
    6. `_initTemp()` — Line (CHT-03)
    7. `_initEntries()` — Scatter (CHT-04)

    Jede _initX()-Funktion:
    - Holt canvas via `document.getElementById('chart-XYZ')`
    - Guard: `if (!canvas) return;`
    - Destroy-before-create: `if (_charts.X) { _charts.X.destroy(); }`
    - Speichert Instanz: `_charts.X = new Chart(canvas, {...})`

    CHT-01 Donut (chart-costs):
    - type: 'doughnut', cutout: '65%'
    - data aus window.TAIWAN.charts.costs (labels, values, colors)
    - borderColor: '#0D0F12', borderWidth: 2, hoverOffset: 6
    - legend: position 'right', color '#E5E7EB', padding 16, fontSize 13
    - tooltip callback: `ctx.label + ': ' + ctx.raw + '%'`

    CHT-02 Bar (chart-nights):
    - type: 'bar'
    - data aus window.TAIWAN.charts.nights (labels, values, colors)
    - borderWidth: 0, borderRadius: 4
    - legend display: false
    - Achsen: x/y ticks color '#9CA3AF', grid color '#1F2937', y stepSize: 1

    CHT-03 Line (chart-temp):
    - type: 'line', zwei datasets: tempMin (cyan #06B6D4) + tempMax (amber #F59E0B)
    - tempMin: fill '+1', backgroundColor 'rgba(6,182,212,0.15)', tension 0.3
    - tempMax: fill false, backgroundColor transparent
    - Achsen: x '#9CA3AF'/'#1F2937', y callback `v + '°C'`

    CHT-04 Scatter (chart-entries):
    - Daten aus window.TAIWAN.charts.entries.data
    - gratisData = entries wo d.gratis===true, map zu {x: globalIndex, y: 0, label: d.label}
    - paidData = entries wo !d.gratis, map zu {x: globalIndex, y: d.eur, label: d.label}
    - Hinweis: globalIndex = Index im Original-Array entries (nicht separater Counter)
      `entries.forEach(function(d, i) { if (d.gratis) gratisData.push({x:i, y:0, label:d.label}); else paidData.push({x:i, y:d.eur, label:d.label}); });`
    - gratis: backgroundColor '#10B981', pointRadius 8
    - paid: backgroundColor '#3B82F6', pointRadius 8
    - x-Achse: display false
    - y-Achse: beginAtZero true, ticks callback `'€' + v`
    - tooltip label callback: raw.label + (datasetIndex===0 ? ' — Gratis' : ' — €' + raw.y)

    CHT-05 Budget-Counter (_renderBudgetCounter):
    - Ziel-ID: 'budget-counter-container'
    - Guard: `var c = document.getElementById('budget-counter-container'); if (!c) return;`
    - Werte: window.TAIWAN.meta.dailyBudget.comfort / .backpacker + meta.budget.low / .high
    - HTML-Struktur: .budget-counter > (.budget-item x2) + .budget-vs + .budget-total
    - Comfort-Item: €145, Label "pro Tag · Komfort"
    - Backpacker-Item: €29, Label "pro Tag · Backpacker-Realität"
    - Total: "Gesamtbudget: €{budget.low}–€{budget.high}"
    - Einmaliger innerHTML-Assignment am Ende

    Wichtig per PATTERNS.md:
    - Chart.js v4 API: kein `Chart.defaults.global.*`
    - `cutout: '65%'` (nicht percentageInnerCutout)
    - Alle Strings als ES5 (kein Template Literal `${}`, kein Arrow Function)
    - window.XModule Pattern (nicht module.exports)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const code=fs.readFileSync('js/charts.js','utf8'); ['window.chartsModule','_charts','chart-costs','chart-nights','chart-temp','chart-entries','budget-counter-container','doughnut','bar','line','scatter','cutout'].forEach(t=>{if(!code.includes(t)){console.error('MISSING: '+t);process.exit(1);}console.log('OK: '+t);})"</automated>
  </verify>
  <acceptance_criteria>
    - js/charts.js ist ein IIFE der `window.chartsModule` setzt
    - `init()` hat Guard für window.TAIWAN
    - Alle 4 Chart-Typen (doughnut/bar/line/scatter) im Code vorhanden
    - `_charts` Objekt für Instance-Tracking vorhanden
    - `destroy()`-Aufruf vor jedem `new Chart()`
    - budget-counter-container HTML-Render vorhanden
    - Kein Template Literal, kein Arrow Function, kein module.exports
    - console.log('[charts.js] initialisiert') am Ende von init()
  </acceptance_criteria>
  <done>js/charts.js implementiert, alle 4 Charts + Budget-Counter codiert, IIFE-Pattern, Guards vorhanden, bereit für Integration in Wave 2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.js → charts.js | Daten aus window.TAIWAN — statisch, kein User-Input, kein Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | window.TAIWAN | accept | Statisches Objekt aus data.js; keine Server-Kommunikation; kein Angriffsvektor im lokalen/GitHub-Pages-Kontext |
</threat_model>

<verification>
Browser: index.html öffnen nach Wave 2 (Integration), Abschnitt #zahlen scrollen — alle 4 Charts + Budget-Counter sichtbar.
</verification>

<success_criteria>
charts.js liefert window.chartsModule.init() der alle 5 Visualisierungen (CHT-01 bis CHT-05) rendert sobald die Canvas-Elemente im DOM vorhanden sind.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-infografiken-videos-restaurants/04-01-SUMMARY.md` erstellen.
</output>
