---
phase: 04-infografiken-videos-restaurants
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/videos.js
autonomous: true
requirements:
  - VID-01
  - VID-02
  - VID-03
  - VID-04

must_haves:
  truths:
    - "9 Tabs (Alle + 8 Kategorien) rendern und filtern korrekt"
    - "20 Video-Cards zeigen Thumbnail oder grauen Fallback bei PLACEHOLDER-IDs"
    - "Click auf PLACEHOLDER-Card öffnet YouTube-Suche mit Videotitel"
    - "Click auf echte ID tauscht Thumbnail gegen eingebettetes iframe mit autoplay"
    - "Grid ist 3-spaltig auf Desktop (>=768px), 1-spaltig auf Mobile"
    - "Tabs wrappe auf Mobile (flex-wrap), kein horizontal scroll"
  artifacts:
    - path: "js/videos.js"
      provides: "window.videosModule IIFE mit init(), Tab-System, Lazy-Load-Grid"
      min_lines: 80
      exports: ["window.videosModule"]
      contains: "videosModule"
  key_links:
    - from: "js/videos.js"
      to: "window.TAIWAN.videos"
      via: "direkter Array-Zugriff"
      pattern: "window\\.TAIWAN\\.videos"
    - from: "vid-card click"
      to: "YouTube embed"
      via: "wrap.innerHTML = iframe"
      pattern: "youtube\\.com/embed"
---

<objective>
`js/videos.js` neu erstellen: YouTube-Video-Grid mit 9 Kategorien-Tabs und Lazy-Load-Pattern. Neue Datei — kein Stub vorhanden.

Purpose: 20 kuratierte YouTube-Videos für die Taiwan-Reise kategorisiert und thumbnail-basiert anzeigen.
Output: `js/videos.js` als IIFE-Modul mit `window.videosModule.init()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-infografiken-videos-restaurants/04-RESEARCH.md
@.planning/phases/04-infografiken-videos-restaurants/04-PATTERNS.md

<interfaces>
<!-- Datenstruktur window.TAIWAN.videos[] aus data.js -->

window.TAIWAN.videos = [
  {
    title: "string",
    youtubeId: "PLACEHOLDER_VID_001",  // alle 20 sind PLACEHOLDERs
    categories: ["ostküste", "familie"], // Array, mehrere möglich
    description: "string"
  },
  // ... 19 weitere
]

<!-- YouTube Thumbnail URL -->
// https://img.youtube.com/vi/{youtubeId}/hqdefault.jpg
// NICHT maxresdefault.jpg (kann 404 geben)
// PLACEHOLDER-IDs liefern 404 → stattdessen .vid-thumb-placeholder verwenden

<!-- YouTube Embed URL (nur bei echten IDs) -->
// https://www.youtube.com/embed/{youtubeId}?autoplay=1

<!-- Container-ID (existiert in index.html, Plan 04-04 ändert nichts daran) -->
// videos-content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: videos.js — IIFE mit Tab-System und Lazy-Load-Grid</name>
  <files>js/videos.js</files>
  <read_first>
    - js/map.js (IIFE-Struktur, Event-Delegation-Pattern, return { init })
    - js/app.js (renderTimeline-Pattern: HTML-String-Building, einmaliger innerHTML-Assignment)
    - .planning/phases/04-infografiken-videos-restaurants/04-PATTERNS.md (Pattern 7 Tab-System, Pattern 6 Lazy-Load, _makeCard, _bindEvents)
  </read_first>
  <action>
    Erstelle `js/videos.js` als neue Datei (nicht vorhanden). IIFE-Pattern wie map.js.

    Modul-Struktur:
    ```
    window.videosModule = (function() {
      'use strict';
      var _currentCat = 'alle';
      // private Hilfsfunktionen
      return { init: function() { ... } };
    })();
    ```

    init():
    1. Guard: `if (!window.TAIWAN || !window.TAIWAN.videos) return;`
    2. `var el = document.getElementById('videos-content'); if (!el) return;`
    3. `el.innerHTML = '';`
    4. `_renderTabs(el);`
    5. `_renderGrid(el, 'alle');`
    6. `_bindEvents(el);`
    7. `console.log('[videos.js] initialisiert');`

    _renderTabs(container):
    - Tabs: 'alle' + alle unique categories aus window.TAIWAN.videos
    - Kategorien ermitteln: forEach video → forEach cat → if indexOf === -1 push
    - Feste Reihenfolge aus RESEARCH.md: ['alle','ostküste','kenting','tempel','xiaoliuqiu','familie','deutsch','transport'] + ev. weitere
    - Besser: dynamisch aus Daten ableiten (kein hardcoding) — 'alle' immer zuerst
    - HTML: `<div class="vid-tabs">` + buttons mit class="vid-tab" + data-cat
    - Aktiver Tab: class "vid-tab--active" auf _currentCat
    - insertAdjacentHTML('beforeend', html) auf container

    _renderGrid(container, cat):
    - Alten Grid entfernen falls vorhanden: `var old = container.querySelector('.vid-grid'); if (old) old.remove();`
    - Filter: cat === 'alle' ? alle Videos : videos wo categories.indexOf(cat) !== -1
    - HTML-String aufbauen: `var html = '<div class="vid-grid">';`
    - forEach filtered: html += _makeCard(video)
    - `html += '</div>';`
    - `container.insertAdjacentHTML('beforeend', html);`

    _makeCard(video):
    - `var isPlaceholder = video.youtubeId.indexOf('PLACEHOLDER') !== -1;`
    - thumbHtml: isPlaceholder
        ? `'<div class="vid-thumb-placeholder"></div>'`
        : `'<img class="vid-thumb" src="https://img.youtube.com/vi/' + video.youtubeId + '/hqdefault.jpg" alt="' + video.title + '" loading="lazy" />'`
    - Return: `'<div class="vid-card" data-youtube-id="' + video.youtubeId + '">' + '<div class="vid-thumb-wrap">' + thumbHtml + '<div class="vid-play-btn" aria-label="Video abspielen">▶</div>' + '</div>' + '<div class="vid-title">' + video.title + '</div>' + '</div>'`

    _bindEvents(container):
    Event Delegation auf container (ein Listener für tabs + cards):
    ```javascript
    container.addEventListener('click', function(e) {
      // Tab-Click
      var tab = e.target.closest('.vid-tab');
      if (tab) {
        _currentCat = tab.getAttribute('data-cat');
        container.querySelectorAll('.vid-tab').forEach(function(t) {
          t.classList.remove('vid-tab--active');
        });
        tab.classList.add('vid-tab--active');
        _renderGrid(container, _currentCat);
        return;
      }
      // Card-Click: Lazy iframe
      var card = e.target.closest('[data-youtube-id]');
      if (!card) return;
      var id = card.getAttribute('data-youtube-id');
      if (id.indexOf('PLACEHOLDER') !== -1) {
        var title = card.querySelector('.vid-title');
        var q = title ? title.textContent : 'Taiwan';
        window.open('https://www.youtube.com/results?search_query=' + encodeURIComponent(q), '_blank');
        return;
      }
      var wrap = card.querySelector('.vid-thumb-wrap');
      if (!wrap) return;
      wrap.innerHTML = '<iframe src="https://www.youtube.com/embed/' + id +
        '?autoplay=1" allow="autoplay; encrypted-media" allowfullscreen></iframe>';
    });
    ```

    Wichtig:
    - Kein Template Literal, kein Arrow Function (ES5)
    - NodeList.forEach: `Array.prototype.forEach.call(...)` falls IE-Kompatibilität nötig,
      aber da moderne Browser via GitHub Pages anvisiert: `.forEach` auf querySelectorAll ist OK
    - Kein iframe beim Seitenload — nur bei Click
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const code=fs.readFileSync('js/videos.js','utf8'); ['window.videosModule','vid-tabs','vid-tab','vid-grid','vid-card','vid-thumb-wrap','vid-thumb-placeholder','vid-play-btn','PLACEHOLDER','youtube.com/embed','youtube.com/results','_currentCat','_renderGrid','_bindEvents'].forEach(t=>{if(!code.includes(t)){console.error('MISSING: '+t);process.exit(1);}console.log('OK: '+t);})"</automated>
  </verify>
  <acceptance_criteria>
    - js/videos.js existiert als neue Datei
    - window.videosModule mit init() exportiert
    - IIFE-Pattern (private scope)
    - _currentCat State-Variable vorhanden
    - Tab-Rendering mit 'alle' als erstem Tab
    - Grid-Rendering mit PLACEHOLDER-Fallback (.vid-thumb-placeholder)
    - Click PLACEHOLDER → window.open YouTube-Suche
    - Click echte ID → iframe in wrap.innerHTML
    - Event Delegation (1 Listener auf container, kein inline onclick)
    - Guard für window.TAIWAN + videos-content Element
  </acceptance_criteria>
  <done>js/videos.js implementiert, Tab-System + Lazy-Load vorhanden, PLACEHOLDER-Handling korrekt, bereit für CSS in Wave 2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.js → videos.js | window.TAIWAN.videos statisch — kein User-Input |
| videos.js → YouTube CDN | Thumbnail-URLs + iframe-Embeds gehen zu YouTube |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | YouTube iframe | accept | Standard Web-Sicherheitsmodell; YouTube-Domain vertrauenswürdig; kein PII übertragen |
| T-04-03 | Tampering | window.open URL | accept | URL aus video.title (statische Daten, kein User-Input); encodeURIComponent verhindert XSS |
</threat_model>

<verification>
Browser: #videos-Section scrollen — Tabs sichtbar, Grid rendert, Klick auf Tab filtert, Klick auf Card öffnet YouTube-Suche (da alle PLACEHOLDER).
</verification>

<success_criteria>
videos.js liefert window.videosModule.init() der 9 Tabs und 20 Video-Cards rendert, PLACEHOLDER-Fallback anzeigt und Event-Delegation korrekt handhabt.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-infografiken-videos-restaurants/04-02-SUMMARY.md` erstellen.
</output>
