---
phase: 07-datenkorrekturen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/app.js
  - js/videos.js
  - css/style.css
autonomous: true
requirements:
  - DAT-01
  - DAT-04

must_haves:
  truths:
    - "Jede Timeline-Phase-Card zeigt avgTemp als Badge im Header (z.B. '🌡 22°C Ø')"
    - "Placeholder-Video-Cards zeigen '🔍 auf YouTube suchen' statt unsichtbarem Play-Button"
  artifacts:
    - path: "js/app.js"
      provides: "avgTemp-Badge in renderPhaseCard()"
      contains: "avgTemp"
    - path: "css/style.css"
      provides: ".tl-temp-badge Stil"
      contains: "tl-temp-badge"
    - path: "js/videos.js"
      provides: "Sichtbares Such-Label bei Placeholder-Cards"
      contains: "auf YouTube suchen"
  key_links:
    - from: "js/data.js phases[].avgTemp"
      to: "js/app.js renderPhaseCard()"
      via: "phase.avgTemp → HTML-String-Konkatenation"
      pattern: "phase.avgTemp"
    - from: "js/videos.js _makeCard()"
      to: "Browser YouTube-Suche"
      via: "isPlaceholder-Zweig zeigt Such-Label, Klick öffnet YouTube"
      pattern: "isPlaceholder"
---

<objective>
Zwei UI-Korrekturen: (1) avgTemp aus data.js als Badge in Timeline-Phase-Card-Header rendern; (2) Placeholder-Video-Cards mit sichtbarem "🔍 auf YouTube suchen"-Label versehen statt unsichtbarem Play-Overlay.

Purpose: Timeline zeigt Temperaturen auf einen Blick; User versteht bei Placeholder-Cards sofort dass Klick eine YouTube-Suche öffnet.
Output: Angepasste app.js, videos.js und style.css.
</objective>

<execution_context>
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/.planning/phases/07-datenkorrekturen/07-02-SUMMARY.md
</execution_context>

<context>
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/CLAUDE.md
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/js/app.js
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/js/videos.js
@/Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/css/style.css

<interfaces>
Aus js/app.js renderPhaseCard() — relevante Stelle (Zeilen 214–225):
```javascript
// Aktueller Stand: Nächte-Badge, dann optional earlyBook-Badge
html += '<span class="tl-badge tl-badge--nights">' + phase.nights + ' Nächte</span>';
if (phase.earlyBook) {
  html += '<span class="tl-badge tl-badge--warn">⚠️ Früh buchen!</span>';
}
// HIER: avgTemp-Badge einfügen (nach dem Nächte-Badge, vor earlyBook-Badge)
// → '<span class="tl-temp-badge">🌡 ' + phase.avgTemp + '°C Ø</span>'
```

Aus js/videos.js _makeCard() — Placeholder-Zweig (Zeilen 36–49):
```javascript
if (isPlaceholder) {
  thumbHtml = '<div class="vid-thumb-placeholder"></div>';
} else { ... }
// Play-Button wird immer gerendert — bei Placeholder unsichtbar da kein Thumb
html += '<div class="vid-play-btn" aria-label="Video abspielen">\u25B6</div>';
// ÄNDERUNG: Wenn isPlaceholder, statt Play-Button ein Such-Label rendern
```

CSS-Konvention (aus css/style.css, bestehende Badges ab Zeile 549):
```css
.tl-badge { display: inline-flex; align-items: center; font-size: 0.7rem; font-weight: 600; border-radius: 4px; padding: 0.15rem 0.45rem; white-space: nowrap; line-height: 1.4; }
/* Neues Badge: .tl-temp-badge — eigene Klasse NICHT tl-badge extends, eigener Style */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: avgTemp-Badge in app.js + CSS-Klasse in style.css</name>
  <files>js/app.js, css/style.css</files>
  <action>
**js/app.js — renderPhaseCard():**

In der Funktion renderPhaseCard() (ab Zeile 194), nach der Zeile:
```
html += '<span class="tl-badge tl-badge--nights">' + phase.nights + ' Nächte</span>';
```
folgende Zeile einfügen (ES5, String-Konkatenation, kein Template-Literal):
```
if (phase.avgTemp !== undefined) {
  html += '<span class="tl-temp-badge">🌡 ' + phase.avgTemp + '°C Ø</span>';
}
```
Das Badge kommt VOR dem earlyBook-Badge. Keine anderen Änderungen in app.js.

**css/style.css — .tl-temp-badge:**

Nach dem Block `.tl-badge--nights { ... }` (endet ca. Zeile 564) einen neuen Block einfügen:
```css
.tl-temp-badge {
  display: inline-flex;
  align-items: center;
  font-size: 0.7rem;
  font-weight: 600;
  border-radius: 4px;
  padding: 0.15rem 0.45rem;
  white-space: nowrap;
  line-height: 1.4;
  background-color: rgba(251,146,60,0.12);
  color: #FDBA74;
  border: 1px solid rgba(251,146,60,0.2);
}
```
Farbe: warmes Orange (passt zu Temperatur-Kontext), konsistent mit Dark Theme.
  </action>
  <verify>
    <automated>grep -c "tl-temp-badge" /Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/js/app.js && grep -c "tl-temp-badge" /Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/css/style.css</automated>
  </verify>
  <done>Mindestens 1 Treffer für "tl-temp-badge" in app.js UND mindestens 1 in style.css.</done>
</task>

<task type="auto">
  <name>Task 2: Placeholder-Card Such-Label in videos.js</name>
  <files>js/videos.js</files>
  <action>
In js/videos.js, Funktion _makeCard() (ab Zeile 35):

Den Placeholder-Zweig ändern. Aktuell:
```javascript
if (isPlaceholder) {
  thumbHtml = '<div class="vid-thumb-placeholder"></div>';
} else { ... }
```

Neu — thumbHtml für Placeholder bekommt das Such-Label eingebettet:
```javascript
if (isPlaceholder) {
  thumbHtml = '<div class="vid-thumb-placeholder">' +
    '<span class="vid-search-label">🔍 auf YouTube suchen</span>' +
    '</div>';
}
```

Den Play-Button-Part anpassen — bei Placeholder keinen Play-Button rendern, bei echten Videos weiterhin:
```javascript
// Statt immer: '<div class="vid-play-btn" ...>\u25B6</div>'
// Neu:
var playBtn = isPlaceholder ? '' : '<div class="vid-play-btn" aria-label="Video abspielen">\u25B6</div>';
```
Dann im return-String: `thumbHtml + playBtn` statt `thumbHtml + '<div class="vid-play-btn"...>'`.

Dazu in css/style.css nach den Video-Styles (suche `.vid-thumb-placeholder`) folgendes ergänzen:
```css
.vid-search-label {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(59,130,246,0.85);
  color: #fff;
  font-size: 0.8rem;
  font-weight: 600;
  text-align: center;
  padding: 0.4rem 0.5rem;
  letter-spacing: 0.01em;
}
```
Damit liegt das Label unten über dem Placeholder-Thumbnail, gut lesbar.

Kein playBtn für Placeholder nötig — Click-Handler in _bindEvents() öffnet bereits YouTube-Suche wenn id PLACEHOLDER enthält.
  </action>
  <verify>
    <automated>grep -c "vid-search-label\|auf YouTube suchen" /Users/mark/Documents/Obsidian/MeinVault/01_Projekte/Winterurlaub-2026/taiwan-dashboard/js/videos.js</automated>
  </verify>
  <done>Mindestens 1 Treffer für "vid-search-label" oder "auf YouTube suchen" in videos.js. Placeholder-Cards zeigen Such-Label, echte Videos zeigen Play-Button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.js phase.avgTemp → DOM | Zahl aus statischer Datei, kein User-Input |
| YouTube Search URL → Browser | Titel-Text aus statischer videos[]-Datei |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | XSS | avgTemp in HTML | accept | Nur Number aus statischer Datei, kein User-Input |
| T-07-04 | XSS | vid-title in YouTube-URL | accept | encodeURIComponent() bereits vorhanden in _bindEvents() |
</threat_model>

<verification>
1. Browser öffnen, Timeline-Section: Jede Phase-Card zeigt "🌡 XX°C Ø" Badge im Header
2. Phase 8 (Alishan) zeigt "🌡 8°C Ø" — erkennbar kälter als andere Phasen
3. Videos-Section: Placeholder-Cards zeigen blaues "🔍 auf YouTube suchen"-Label
4. Klick auf Placeholder-Card öffnet YouTube-Suche (wie vorher)
5. Klick auf echte Video-Card startet Inline-Player (unverändert)
</verification>

<success_criteria>
- avgTemp-Badge erscheint in allen 10 Timeline-Cards
- Placeholder-Cards zeigen "🔍 auf YouTube suchen" statt leerem Overlay
- Echte Video-Cards unverändert (Play-Button + Inline-Player)
- Keine JS-Fehler in Browser-Konsole
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/07-datenkorrekturen/07-02-SUMMARY.md` erstellen.
</output>
